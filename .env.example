# DockWINterface Environment Configuration
# Copy this file to .env and configure your values

# Required: Session secret for Flask application
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=your-secure-random-session-key-here

# Optional: OpenAI API key for AI assistant functionality
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Flask environment
FLASK_ENV=production

# Optional: Custom Docker host (if not using local socket)
# DOCKER_HOST=tcp://docker-host:2375